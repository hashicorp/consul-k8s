name: reusable-nightly-cloud-releases

on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string
      additional-flags:
        required: false
        type: string
        default: ""
      directory:
        required: true
        type: string
      gotestsum-version:
        required: true
        type: string
      kind-version:
        required: false
        type: string
        default: "v1.24.6"
      checkout-ref:
        required: false
        type: string
        default: ${{ github.sha }}
      terraform-directory:
        required: false
        type: string
        default: charts/consul/test/terraform/gke
      consul-helm-test-image:
        required: false
        type: string
        default: docker.mirror.hashicorp.services/hashicorpdev/consul-helm-test:0.15.0
      debug_enabled:
        description: 'Start tmate session if any step fails'
        required: false
        type: boolean
        default: false  # GitHub parses this value to string, see https://github.com/actions/runner/issues/1483
      debug_timeout_minutes:
        description: 'How many minutes should the tmate session close itself after?'
        required: false
        type: string  # No support for numeric value
        default: '10'
    secrets:
      CONSUL_ENT_LICENSE:
        required: true
      DOCKER_USER:
        required: true
      DOCKER_PASS:
        required: true
      GOOGLE_CREDENTIALS:
        required: true
      CLOUDSDK_CORE_PROJECT:
        required: true

# Environment variables can only be used at the step level
env:
  TEST_RESULTS: /tmp/test-results # path to where test results are saved
  CONSUL_ENT_LICENSE: ${{ secrets.CONSUL_ENT_LICENSE }}

jobs:
  get-go-version:
    name: "Determine Go toolchain version"
    runs-on: ubuntu-latest
    env:
      output-name: go-version
    outputs:
      go-version: ${{ steps.get-go-version.outputs.go-version }}
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ inputs.checkout-ref }}
      - name: Determine Go version
        id: get-go-version
        # We use .go-version as our source of truth for current Go
        # version, because "goenv" can react to it automatically.
        run: |
          echo "Building with Go $(cat .go-version)"
          echo "::set-output name=${{env.output-name}}::$(cat .go-version)"

  get-product-version:
    runs-on: ubuntu-latest
    outputs:
      product-version: ${{ steps.get-product-version.outputs.product-version }}
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ inputs.checkout-ref }}
      - name: get product version
        id: get-product-version
        run: |
          make version
          echo "::set-output name=product-version::$(make version)"

  build-distros:
    needs: [get-go-version, get-product-version]
    runs-on: ubuntu-latest
    env:
      version: ${{needs.get-product-version.outputs.product-version}}
    strategy:
      matrix:
        include:
          # cli
          - {go: "${{ needs.get-go-version.outputs.go-version }}", goos: "linux", goarch: "amd64", component: "cli", pkg_name: "consul-k8s", "bin_name": "consul-k8s" }
          # control-plane
          - {go: "${{ needs.get-go-version.outputs.go-version }}", goos: "linux", goarch: "amd64", component: "control-plane", pkg_name: "consul-k8s-control-plane", "bin_name": "consul-k8s-control-plane" }
          # consul-cni
          - {go: "${{ needs.get-go-version.outputs.go-version }}", goos: "linux", goarch: "amd64", component: "control-plane/cni", pkg_name: "consul-cni", "bin_name": "consul-cni" }

      fail-fast: true

    name: Go ${{ matrix.go }} ${{ matrix.goos }} ${{ matrix.goarch }} ${{ matrix.component }} build
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ inputs.checkout-ref }}
      - name: Setup go
        uses: actions/setup-go@v3
        with:
          go-version: ${{ matrix.go }}

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        working-directory: ${{ matrix.component }}
        run: |
          mkdir -p dist out

          export GIT_COMMIT=$(git rev-parse --short HEAD)
          export GIT_DIRTY=$(test -n "$(git status --porcelain)" && echo "+CHANGES")
          export GIT_IMPORT=github.com/hashicorp/consul-k8s/${{ matrix.component }}/version
          export GOLDFLAGS="-X ${GIT_IMPORT}.GitCommit=${GIT_COMMIT}${GIT_DIRTY} -X ${GIT_IMPORT}.GitDescribe=${{ env.version }}"


          CGO_ENABLED=0 go build -o dist/${{ matrix.bin_name }} -ldflags "${GOLDFLAGS}" .
          zip -r -j out/${{ matrix.pkg_name }}_${{ env.version }}_${{ matrix.goos }}_${{ matrix.goarch }}.zip dist/

      - name: Upload built binaries
        uses: actions/upload-artifact@v3
        with:
          name: ${{ matrix.pkg_name }}_${{ env.version }}_${{ matrix.goos }}_${{ matrix.goarch }}.zip
          path: ${{ matrix.component}}/out/${{ matrix.pkg_name }}_${{ env.version }}_${{ matrix.goos }}_${{ matrix.goarch }}.zip

  # upload dev docker image
  dev-upload-docker:
    if: github.repository_owner == 'hashicorp' # Do not run on forks as this requires secrets
    needs: [ get-product-version, build-distros ]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: ["amd64"]
    env:
      repo: ${{ github.event.repository.name }}
      version: ${{ needs.get-product-version.outputs.product-version }}
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ inputs.checkout-ref }}
      - uses: actions/download-artifact@v3
        with:
          name: consul-cni_${{ env.version }}_linux_${{ matrix.arch }}.zip
          path: control-plane/dist/cni/linux/${{ matrix.arch }}
      - uses: actions/download-artifact@v3
        with:
          name: consul-k8s-control-plane_${{ env.version }}_linux_${{ matrix.arch }}.zip
          path: control-plane/dist/linux/${{ matrix.arch }}
      - name: extract consul-cni zip
        env:
          ZIP_LOCATION: control-plane/dist/cni/linux/${{ matrix.arch }}
        run: |
          cd "${ZIP_LOCATION}"
          unzip -j *.zip
      - name: extract control-plane zip
        env:
          ZIP_LOCATION: control-plane/dist/linux/${{ matrix.arch }}
        run: |
          cd "${ZIP_LOCATION}"
          unzip -j *.zip
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}
      - name: Docker Build (Action)
        uses: docker/build-push-action@v3
        with:
          push: true
          context: control-plane
          platforms: ${{ matrix.arch }}
          target: release-default
          tags: docker.io/hashicorppreview/${{ env.repo }}-control-plane:${{ env.version }}-${{ inputs.name }}-${{ github.sha }}

  cleanup:
    runs-on: ubuntu-latest
    container:
      image: ${{ inputs.consul-helm-test-image }}
      options: --user root
      credentials:
        username: ${{ secrets.DOCKER_USER }}
        password: ${{ secrets.DOCKER_PASS }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.checkout-ref }}
      - id: 'auth'
        uses: 'google-github-actions/auth@v0'
        with:
          credentials_json: '${{ secrets.GOOGLE_CREDENTIALS }}'
      - name: cleanup leftover resources
        run: |
          clusters=$(gcloud container clusters list --zone us-central1-a --project ${{ secrets.CLOUDSDK_CORE_PROJECT }} --format json  | jq -r '.[] | select(.name | test("^consul-k8s-\\d+$")) | .name')
          for cluster in $clusters; do
          echo "Deleting $cluster GKE cluster"
          gcloud container clusters delete $cluster --zone us-central1-a --project ${{ secrets.CLOUDSDK_CORE_PROJECT }} --quiet
          done

  acceptance:
    needs: [ get-product-version, dev-upload-docker, get-go-version, cleanup]
    runs-on: [custom, linux, xl]
    container:
      image: ${{ inputs.consul-helm-test-image }}
      credentials:
        username: ${{ secrets.DOCKER_USER }}
        password: ${{ secrets.DOCKER_PASS }}
      options: --user root # Need root access for some of the actions to work, container does not have an entry point with root privileges.
      env:
        actor: ${{ github.actor }}
    strategy:
      matrix:
        include:
          - {runner: "0", test-packages: "basic consul-dns"} # TODO: Remove when metrics tests are fixed
          #          - {runner: "0", test-packages: "basic consul-dns metrics"} # TODO: re-enable acceptance tests, currently waiting on fix for metrics
#          - {runner: "1", test-packages: "connect"}
#          - {runner: "2", test-packages: "controller example"}
#          - {runner: "3", test-packages: "ingress-gateway"}
#          - {runner: "4", test-packages: "partitions"}
#          - {runner: "5", test-packages: "peering"}
#          - {runner: "6", test-packages: "snapshot-agent vault wan-federation"}
#          - {runner: "7", test-packages: "cli sync terminating-gateway"}

      fail-fast: false
    steps:
      - name: Set ownership
        run: |
          # this is to fix GIT not setting ownership when starting the docker container
          sudo chown -R $(id -u):$(id -g) $PWD

      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.checkout-ref }}

      - name: Setup go
        uses: actions/setup-go@v3
        with:
          go-version: ${{ inputs.go-version }}

      - name: Setup go mod cache
        uses: actions/cache@v3
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Install pre-requisites # Install gotestsum, kind, kubectl, and helm
        #shell: bash # By default containers do not use `bash` and instead use `sh`. Changing to bash to be consistent with non container runner
        run: |
          wget https://github.com/gotestyourself/gotestsum/releases/download/v1.6.4/gotestsum_1.6.4_linux_amd64.tar.gz
          sudo tar -C /usr/local/bin -xzf gotestsum_1.6.4_linux_amd64.tar.gz
          rm gotestsum_1.6.4_linux_amd64.tar.gz

      - run: mkdir -p ${{ env.TEST_RESULTS }}

      - name: go mod download
        working-directory: ${{ inputs.directory }}
        run: go mod download

      - name: Build CLI
        run: |
          make cli-dev
          consul-k8s version

      - id: 'auth'
        uses: 'google-github-actions/auth@v0'
        with:
          credentials_json: '${{ secrets.GOOGLE_CREDENTIALS }}'

      - name: terraform init & apply
        working-directory: ${{ inputs.terraform-directory }}
        #shell: bash
        run: |
          terraform init

          # On GKE, we're setting the build number instead of build URL because label values
          # cannot contain '/'.
          terraform apply \
            -var project=${{ secrets.CLOUDSDK_CORE_PROJECT }} \
            -var init_cli=true \
            -var cluster_count=2 \
            -var labels="{\"build_number\": \"$(git rev-list --count HEAD)\"}" \
            -auto-approve

          primary_kubeconfig=$(terraform output -json | jq -r .kubeconfigs.value[0])
          secondary_kubeconfig=$(terraform output -json | jq -r .kubeconfigs.value[1])

          echo "primary_kubeconfig=$primary_kubeconfig" >> $GITHUB_ENV
          echo "secondary_kubeconfig=$secondary_kubeconfig" >> $GITHUB_ENV

      # We have to run the tests for each package separately so that we can
      # exit early if any test fails (-failfast only works within a single
      # package).
      - name: Run acceptance tests ${{ matrix.runner }}
        working-directory: ${{ inputs.directory }}
        if: github.repository_owner == 'hashicorp' # This prevents running on forks
        shell: bash
        run: |
          exit_code=0
          echo "Running packages: ${{ matrix.test-packages }}"
          for pkg in $(echo ${{ matrix.test-packages }})
          do
            fullpkg="github.com/hashicorp/consul-k8s/${{ inputs.directory }}/${pkg}"
            echo "Testing package: ${fullpkg}"
            if ! go test ${fullpkg} -p 1 -timeout 2h -failfast \
              ${{ inputs.additional-flags }} \
              -enable-enterprise \
              -enable-multi-cluster \
              -debug-directory=${{ env.TEST_RESULTS }}/debug \
              -consul-k8s-image=docker.io/hashicorppreview/${{ github.event.repository.name }}-control-plane:${{ needs.get-product-version.outputs.product-version }}-pr-${{ github.sha }} \
              -kubeconfig="$(eval echo $primary_kubeconfig)" \
              -secondary-kubeconfig="$(eval echo $secondary_kubeconfig)"
            then
              echo "Tests in ${pkg} failed, aborting early"
              exit_code=1
              break
            fi
          done
          gotestsum --raw-command --junitfile "${{ env.TEST_RESULTS }}/gotestsum-report.xml" -- cat jsonfile*
          exit $exit_code

      - name: Upload tests
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: ${{ inputs.name }}-${{ matrix.test-packages }}-gotestsum-report.xml
          path: ${{ env.TEST_RESULTS }}/gotestsum-report.xml

      - name: Upload debug (on failure)
        if: failure()
        uses: actions/upload-artifact@v3
        with:
          name: ${{ inputs.name }}-${{ matrix.test-packages }}-debug-info
          path: ${{ env.TEST_RESULTS }}/debug

      - name: terraform destroy
        if: always()
        working-directory: ${{ inputs.terraform-directory }}
        shell: bash
        run: |
          terraform destroy -var project=${CLOUDSDK_CORE_PROJECT} -auto-approve
          
