# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.1
  name: gatewayclassconfigs.mesh.consul.hashicorp.com
spec:
  group: mesh.consul.hashicorp.com
  names:
    kind: GatewayClassConfig
    listKind: GatewayClassConfigList
    plural: gatewayclassconfigs
    singular: gatewayclassconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: The age of the resource
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v2beta1
    schema:
      openAPIV3Schema:
        description: GatewayClassConfig is the Schema for the Mesh Gateway API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GatewayClassConfigSpec specifies the desired state of the
              GatewayClassConfig CRD.
            properties:
              annotations:
                description: Annotations are applied to the created resource
                properties:
                  inheritFromGateway:
                    description: InheritFromGateway lists the names/keys of annotations
                      or labels to copy from the Gateway resource. Any name/key included
                      here will override those in Set if specified on the Gateway.
                    items:
                      type: string
                    type: array
                  set:
                    additionalProperties:
                      type: string
                    description: Set lists the names/keys and values of annotations
                      or labels to set on the resource. Any name/key included here
                      will be overridden if present in InheritFromGateway and set
                      on the Gateway.
                    type: object
                type: object
              deployment:
                description: Deployment contains config specific to the Deployment
                  created from this GatewayClass
                properties:
                  annotations:
                    description: Annotations are applied to the created resource
                    properties:
                      inheritFromGateway:
                        description: InheritFromGateway lists the names/keys of annotations
                          or labels to copy from the Gateway resource. Any name/key
                          included here will override those in Set if specified on
                          the Gateway.
                        items:
                          type: string
                        type: array
                      set:
                        additionalProperties:
                          type: string
                        description: Set lists the names/keys and values of annotations
                          or labels to set on the resource. Any name/key included
                          here will be overridden if present in InheritFromGateway
                          and set on the Gateway.
                        type: object
                    type: object
                  container:
                    description: Container contains config specific to the created
                      Deployment's container
                    properties:
                      consul:
                        description: Consul specifies configuration for the consul-dataplane
                          container
                        properties:
                          logging:
                            description: Logging specifies the logging configuration
                              for Consul Dataplane
                            properties:
                              level:
                                description: Level sets the logging level for Consul
                                  Dataplane (debug, info, etc.)
                                type: string
                            type: object
                        type: object
                      portModifier:
                        description: PortModifier specifies the value to be added
                          to every port value for listeners on this gateway
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for the created Deployment's container
                        properties:
                          claims:
                            description: "Claims lists the names of resources, defined
                              in spec.resourceClaims, that are used by this container.
                              \n This is an alpha field and requires enabling the
                              DynamicResourceAllocation feature gate. \n This field
                              is immutable. It can only be set for containers."
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: Name must match the name of one entry
                                    in pod.spec.resourceClaims of the Pod where this
                                    field is used. It makes that resource available
                                    inside a container.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Limits describes the maximum amount of compute
                              resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Requests describes the minimum amount of
                              compute resources required. If Requests is omitted for
                              a container, it defaults to Limits if that is explicitly
                              specified, otherwise to an implementation-defined value.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                        type: object
                    type: object
                  initContainer:
                    description: InitContainer contains config specific to the created
                      Deployment's init container
                    properties:
                      consul:
                        description: Consul specifies configuration for the consul-k8s-control-plane
                          init container
                        properties:
                          logging:
                            description: Logging specifies the logging configuration
                              for Consul Dataplane
                            properties:
                              level:
                                description: Level sets the logging level for Consul
                                  Dataplane (debug, info, etc.)
                                type: string
                            type: object
                        type: object
                      resources:
                        description: Resources specifies the resource requirements
                          for the created Deployment's init container
                        properties:
                          claims:
                            description: "Claims lists the names of resources, defined
                              in spec.resourceClaims, that are used by this container.
                              \n This is an alpha field and requires enabling the
                              DynamicResourceAllocation feature gate. \n This field
                              is immutable. It can only be set for containers."
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: Name must match the name of one entry
                                    in pod.spec.resourceClaims of the Pod where this
                                    field is used. It makes that resource available
                                    inside a container.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Limits describes the maximum amount of compute
                              resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Requests describes the minimum amount of
                              compute resources required. If Requests is omitted for
                              a container, it defaults to Limits if that is explicitly
                              specified, otherwise to an implementation-defined value.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                        type: object
                    type: object
                  labels:
                    description: Labels are applied to the created resource
                    properties:
                      inheritFromGateway:
                        description: InheritFromGateway lists the names/keys of annotations
                          or labels to copy from the Gateway resource. Any name/key
                          included here will override those in Set if specified on
                          the Gateway.
                        items:
                          type: string
                        type: array
                      set:
                        additionalProperties:
                          type: string
                        description: Set lists the names/keys and values of annotations
                          or labels to set on the resource. Any name/key included
                          here will be overridden if present in InheritFromGateway
                          and set on the Gateway.
                        type: object
                    type: object
                  nodeSelector:
                    description: NodeSelector specifies the node selector to use on
                      the created Deployment
                    properties:
                      nodeSelectorTerms:
                        description: Required. A list of node selector terms. The
                          terms are ORed.
                        items:
                          description: A null or empty node selector term matches
                            no objects. The requirements of them are ANDed. The TopologySelectorTerm
                            type implements a subset of the NodeSelectorTerm.
                          properties:
                            matchExpressions:
                              description: A list of node selector requirements by
                                node's labels.
                              items:
                                description: A node selector requirement is a selector
                                  that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: Represents a key's relationship to
                                      a set of values. Valid operators are In, NotIn,
                                      Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: An array of string values. If the
                                      operator is In or NotIn, the values array must
                                      be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator
                                      is Gt or Lt, the values array must have a single
                                      element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge
                                      patch.
                                    items:
                                      type: string
                                    type: array
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                            matchFields:
                              description: A list of node selector requirements by
                                node's fields.
                              items:
                                description: A node selector requirement is a selector
                                  that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: Represents a key's relationship to
                                      a set of values. Valid operators are In, NotIn,
                                      Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: An array of string values. If the
                                      operator is In or NotIn, the values array must
                                      be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator
                                      is Gt or Lt, the values array must have a single
                                      element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge
                                      patch.
                                    items:
                                      type: string
                                    type: array
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                          type: object
                          x-kubernetes-map-type: atomic
                        type: array
                    required:
                    - nodeSelectorTerms
                    type: object
                    x-kubernetes-map-type: atomic
                  priorityClassName:
                    description: PriorityClassName specifies the priority class name
                      to use on the created Deployment
                    type: string
                  replicas:
                    description: Replicas specifies the configuration to control the
                      number of replicas for the created Deployment
                    properties:
                      default:
                        description: Default is the number of replicas assigned to
                          the Deployment when created
                        format: int32
                        type: integer
                      max:
                        description: Max is the maximum number of replicas allowed
                          for a gateway with this class. If the replica count exceeds
                          this value due to manual or automated scaling, the replica
                          count will be restored to this value.
                        format: int32
                        type: integer
                      min:
                        description: Min is the minimum number of replicas allowed
                          for a gateway with this class. If the replica count drops
                          below this value due to manual or automated scaling, the
                          replica count will be restored to this value.
                        format: int32
                        type: integer
                    type: object
                  securityContext:
                    description: SecurityContext specifies the security context for
                      the created Deployment's Pod
                    properties:
                      fsGroup:
                        description: "A special supplemental group that applies to
                          all containers in a pod. Some volume types allow the Kubelet
                          to change the ownership of that volume to be owned by the
                          pod: \n 1. The owning GID will be the FSGroup 2. The setgid
                          bit is set (new files created in the volume will be owned
                          by FSGroup) 3. The permission bits are OR'd with rw-rw----
                          \n If unset, the Kubelet will not modify the ownership and
                          permissions of any volume. Note that this field cannot be
                          set when spec.os.name is windows."
                        format: int64
                        type: integer
                      fsGroupChangePolicy:
                        description: 'fsGroupChangePolicy defines behavior of changing
                          ownership and permission of the volume before being exposed
                          inside Pod. This field will only apply to volume types which
                          support fsGroup based ownership(and permissions). It will
                          have no effect on ephemeral volume types such as: secret,
                          configmaps and emptydir. Valid values are "OnRootMismatch"
                          and "Always". If not specified, "Always" is used. Note that
                          this field cannot be set when spec.os.name is windows.'
                        type: string
                      runAsGroup:
                        description: The GID to run the entrypoint of the container
                          process. Uses runtime default if unset. May also be set
                          in SecurityContext.  If set in both SecurityContext and
                          PodSecurityContext, the value specified in SecurityContext
                          takes precedence for that container. Note that this field
                          cannot be set when spec.os.name is windows.
                        format: int64
                        type: integer
                      runAsNonRoot:
                        description: Indicates that the container must run as a non-root
                          user. If true, the Kubelet will validate the image at runtime
                          to ensure that it does not run as UID 0 (root) and fail
                          to start the container if it does. If unset or false, no
                          such validation will be performed. May also be set in SecurityContext.  If
                          set in both SecurityContext and PodSecurityContext, the
                          value specified in SecurityContext takes precedence.
                        type: boolean
                      runAsUser:
                        description: The UID to run the entrypoint of the container
                          process. Defaults to user specified in image metadata if
                          unspecified. May also be set in SecurityContext.  If set
                          in both SecurityContext and PodSecurityContext, the value
                          specified in SecurityContext takes precedence for that container.
                          Note that this field cannot be set when spec.os.name is
                          windows.
                        format: int64
                        type: integer
                      seLinuxOptions:
                        description: The SELinux context to be applied to all containers.
                          If unspecified, the container runtime will allocate a random
                          SELinux context for each container.  May also be set in
                          SecurityContext.  If set in both SecurityContext and PodSecurityContext,
                          the value specified in SecurityContext takes precedence
                          for that container. Note that this field cannot be set when
                          spec.os.name is windows.
                        properties:
                          level:
                            description: Level is SELinux level label that applies
                              to the container.
                            type: string
                          role:
                            description: Role is a SELinux role label that applies
                              to the container.
                            type: string
                          type:
                            description: Type is a SELinux type label that applies
                              to the container.
                            type: string
                          user:
                            description: User is a SELinux user label that applies
                              to the container.
                            type: string
                        type: object
                      seccompProfile:
                        description: The seccomp options to use by the containers
                          in this pod. Note that this field cannot be set when spec.os.name
                          is windows.
                        properties:
                          localhostProfile:
                            description: localhostProfile indicates a profile defined
                              in a file on the node should be used. The profile must
                              be preconfigured on the node to work. Must be a descending
                              path, relative to the kubelet's configured seccomp profile
                              location. Must only be set if type is "Localhost".
                            type: string
                          type:
                            description: "type indicates which kind of seccomp profile
                              will be applied. Valid options are: \n Localhost - a
                              profile defined in a file on the node should be used.
                              RuntimeDefault - the container runtime default profile
                              should be used. Unconfined - no profile should be applied."
                            type: string
                        required:
                        - type
                        type: object
                      supplementalGroups:
                        description: A list of groups applied to the first process
                          run in each container, in addition to the container's primary
                          GID, the fsGroup (if specified), and group memberships defined
                          in the container image for the uid of the container process.
                          If unspecified, no additional groups are added to any container.
                          Note that group memberships defined in the container image
                          for the uid of the container process are still effective,
                          even if they are not included in this list. Note that this
                          field cannot be set when spec.os.name is windows.
                        items:
                          format: int64
                          type: integer
                        type: array
                      sysctls:
                        description: Sysctls hold a list of namespaced sysctls used
                          for the pod. Pods with unsupported sysctls (by the container
                          runtime) might fail to launch. Note that this field cannot
                          be set when spec.os.name is windows.
                        items:
                          description: Sysctl defines a kernel parameter to be set
                          properties:
                            name:
                              description: Name of a property to set
                              type: string
                            value:
                              description: Value of a property to set
                              type: string
                          required:
                          - name
                          - value
                          type: object
                        type: array
                      windowsOptions:
                        description: The Windows specific settings applied to all
                          containers. If unspecified, the options within a container's
                          SecurityContext will be used. If set in both SecurityContext
                          and PodSecurityContext, the value specified in SecurityContext
                          takes precedence. Note that this field cannot be set when
                          spec.os.name is linux.
                        properties:
                          gmsaCredentialSpec:
                            description: GMSACredentialSpec is where the GMSA admission
                              webhook (https://github.com/kubernetes-sigs/windows-gmsa)
                              inlines the contents of the GMSA credential spec named
                              by the GMSACredentialSpecName field.
                            type: string
                          gmsaCredentialSpecName:
                            description: GMSACredentialSpecName is the name of the
                              GMSA credential spec to use.
                            type: string
                          hostProcess:
                            description: HostProcess determines if a container should
                              be run as a 'Host Process' container. This field is
                              alpha-level and will only be honored by components that
                              enable the WindowsHostProcessContainers feature flag.
                              Setting this field without the feature flag will result
                              in errors when validating the Pod. All of a Pod's containers
                              must have the same effective HostProcess value (it is
                              not allowed to have a mix of HostProcess containers
                              and non-HostProcess containers).  In addition, if HostProcess
                              is true then HostNetwork must also be set to true.
                            type: boolean
                          runAsUserName:
                            description: The UserName in Windows to run the entrypoint
                              of the container process. Defaults to the user specified
                              in image metadata if unspecified. May also be set in
                              PodSecurityContext. If set in both SecurityContext and
                              PodSecurityContext, the value specified in SecurityContext
                              takes precedence.
                            type: string
                        type: object
                    type: object
                  tolerations:
                    description: Tolerations specifies the tolerations to use on the
                      created Deployment
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              labels:
                description: Labels are applied to the created resource
                properties:
                  inheritFromGateway:
                    description: InheritFromGateway lists the names/keys of annotations
                      or labels to copy from the Gateway resource. Any name/key included
                      here will override those in Set if specified on the Gateway.
                    items:
                      type: string
                    type: array
                  set:
                    additionalProperties:
                      type: string
                    description: Set lists the names/keys and values of annotations
                      or labels to set on the resource. Any name/key included here
                      will be overridden if present in InheritFromGateway and set
                      on the Gateway.
                    type: object
                type: object
              role:
                description: Role contains config specific to the Role created from
                  this GatewayClass
                properties:
                  annotations:
                    description: Annotations are applied to the created resource
                    properties:
                      inheritFromGateway:
                        description: InheritFromGateway lists the names/keys of annotations
                          or labels to copy from the Gateway resource. Any name/key
                          included here will override those in Set if specified on
                          the Gateway.
                        items:
                          type: string
                        type: array
                      set:
                        additionalProperties:
                          type: string
                        description: Set lists the names/keys and values of annotations
                          or labels to set on the resource. Any name/key included
                          here will be overridden if present in InheritFromGateway
                          and set on the Gateway.
                        type: object
                    type: object
                  labels:
                    description: Labels are applied to the created resource
                    properties:
                      inheritFromGateway:
                        description: InheritFromGateway lists the names/keys of annotations
                          or labels to copy from the Gateway resource. Any name/key
                          included here will override those in Set if specified on
                          the Gateway.
                        items:
                          type: string
                        type: array
                      set:
                        additionalProperties:
                          type: string
                        description: Set lists the names/keys and values of annotations
                          or labels to set on the resource. Any name/key included
                          here will be overridden if present in InheritFromGateway
                          and set on the Gateway.
                        type: object
                    type: object
                type: object
              roleBinding:
                description: RoleBinding contains config specific to the RoleBinding
                  created from this GatewayClass
                properties:
                  annotations:
                    description: Annotations are applied to the created resource
                    properties:
                      inheritFromGateway:
                        description: InheritFromGateway lists the names/keys of annotations
                          or labels to copy from the Gateway resource. Any name/key
                          included here will override those in Set if specified on
                          the Gateway.
                        items:
                          type: string
                        type: array
                      set:
                        additionalProperties:
                          type: string
                        description: Set lists the names/keys and values of annotations
                          or labels to set on the resource. Any name/key included
                          here will be overridden if present in InheritFromGateway
                          and set on the Gateway.
                        type: object
                    type: object
                  labels:
                    description: Labels are applied to the created resource
                    properties:
                      inheritFromGateway:
                        description: InheritFromGateway lists the names/keys of annotations
                          or labels to copy from the Gateway resource. Any name/key
                          included here will override those in Set if specified on
                          the Gateway.
                        items:
                          type: string
                        type: array
                      set:
                        additionalProperties:
                          type: string
                        description: Set lists the names/keys and values of annotations
                          or labels to set on the resource. Any name/key included
                          here will be overridden if present in InheritFromGateway
                          and set on the Gateway.
                        type: object
                    type: object
                type: object
              service:
                description: Service contains config specific to the Service created
                  from this GatewayClass
                properties:
                  annotations:
                    description: Annotations are applied to the created resource
                    properties:
                      inheritFromGateway:
                        description: InheritFromGateway lists the names/keys of annotations
                          or labels to copy from the Gateway resource. Any name/key
                          included here will override those in Set if specified on
                          the Gateway.
                        items:
                          type: string
                        type: array
                      set:
                        additionalProperties:
                          type: string
                        description: Set lists the names/keys and values of annotations
                          or labels to set on the resource. Any name/key included
                          here will be overridden if present in InheritFromGateway
                          and set on the Gateway.
                        type: object
                    type: object
                  labels:
                    description: Labels are applied to the created resource
                    properties:
                      inheritFromGateway:
                        description: InheritFromGateway lists the names/keys of annotations
                          or labels to copy from the Gateway resource. Any name/key
                          included here will override those in Set if specified on
                          the Gateway.
                        items:
                          type: string
                        type: array
                      set:
                        additionalProperties:
                          type: string
                        description: Set lists the names/keys and values of annotations
                          or labels to set on the resource. Any name/key included
                          here will be overridden if present in InheritFromGateway
                          and set on the Gateway.
                        type: object
                    type: object
                  type:
                    description: Type specifies the type of Service to use (LoadBalancer,
                      ClusterIP, etc.)
                    type: string
                type: object
              serviceAccount:
                description: ServiceAccount contains config specific to the corev1.ServiceAccount
                  created from this GatewayClass
                properties:
                  annotations:
                    description: Annotations are applied to the created resource
                    properties:
                      inheritFromGateway:
                        description: InheritFromGateway lists the names/keys of annotations
                          or labels to copy from the Gateway resource. Any name/key
                          included here will override those in Set if specified on
                          the Gateway.
                        items:
                          type: string
                        type: array
                      set:
                        additionalProperties:
                          type: string
                        description: Set lists the names/keys and values of annotations
                          or labels to set on the resource. Any name/key included
                          here will be overridden if present in InheritFromGateway
                          and set on the Gateway.
                        type: object
                    type: object
                  labels:
                    description: Labels are applied to the created resource
                    properties:
                      inheritFromGateway:
                        description: InheritFromGateway lists the names/keys of annotations
                          or labels to copy from the Gateway resource. Any name/key
                          included here will override those in Set if specified on
                          the Gateway.
                        items:
                          type: string
                        type: array
                      set:
                        additionalProperties:
                          type: string
                        description: Set lists the names/keys and values of annotations
                          or labels to set on the resource. Any name/key included
                          here will be overridden if present in InheritFromGateway
                          and set on the Gateway.
                        type: object
                    type: object
                type: object
            type: object
          status:
            properties:
              conditions:
                description: Conditions indicate the latest available observations
                  of a resource's current state.
                items:
                  description: 'Conditions define a readiness condition for a Consul
                    resource. See: https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#typical-status-properties'
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              lastSyncedTime:
                description: LastSyncedTime is the last time the resource successfully
                  synced with Consul.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
