# ROSA Terraform

This module creates two AWS VPCs, peers them, provisions the basic routing needed for cross-VPC connectivity, and then creates two ROSA clusters with the `rosa` CLI into the generated BYOVPC subnet sets.

It is designed for Consul multi-cluster testing where the clusters need direct cross-VPC reachability for the Consul server plane.

## Prerequisites

1. `terraform`, `aws`, `rosa`, and `jq` must be installed.
2. `aws` must already be authenticated for the target account.
3. `rosa login` must already be completed.
4. The AWS account must be prepared for ROSA STS clusters. This module uses `rosa create cluster --sts --mode auto`.
5. The chosen VPC, service, and pod CIDRs must not overlap.
6. When `az_count = 1`, the module builds a single-AZ ROSA layout and passes one private plus one public subnet per cluster. When `az_count > 1`, it adds `--multi-az` and passes all generated subnets for multi-AZ BYOVPC creation.

## What It Creates

1. A primary AWS VPC with public and private subnets.
2. A secondary AWS VPC with public and private subnets.
3. An AWS VPC peering connection with routes in both directions.
4. Two ROSA clusters, one per VPC.
5. Each ROSA cluster uses all generated subnets for the selected topology: one private plus one public subnet for single-AZ, or one private plus one public subnet per AZ for multi-AZ.
6. Ingress rules on each cluster's worker security group to allow Consul server-plane traffic from the peer VPC.

## Usage

```bash
cd charts/consul/test/terraform/rosa
terraform init
terraform apply \
  -var primary_cluster_name=consul-rosa-east \
  -var secondary_cluster_name=consul-rosa-west
```

## Test Bed Example

For an east/west ROSA layout matching the current test-bed naming convention, copy the example tfvars and adjust CIDRs only if they overlap with existing routed networks:

```bash
cd charts/consul/test/terraform/rosa
cp test-bed-east-west.tfvars.example test-bed-east-west.tfvars
terraform init
terraform apply -var-file=test-bed-east-west.tfvars
```

The example file sets:

1. `primary_cluster_name = "test-bed-east"`
2. `secondary_cluster_name = "test-bed-west"`
3. `primary_region = "us-east-2"`
4. `secondary_region = "us-west-2"`
5. non-overlapping example VPC, pod, and service CIDRs
6. `az_count = 1` for a single-AZ test bed
7. `--channel-group stable` for both ROSA clusters

## Important Notes

1. This module discovers the ROSA node security groups from the cluster infra ID, VPC, and ROSA cluster tags rather than assuming a fixed `worker-sg` name.
2. It opens worker-node ingress from the peer VPC CIDR for Consul ports `8300`, `8301/tcp`, `8301/udp`, `8501`, and `8502`.
3. It does not manage application-level Consul installation. It only builds the network and ROSA cluster base.
4. `terraform destroy` sends `rosa delete cluster` for both clusters but does not wait for the asynchronous deletion to finish before Terraform continues tearing down AWS resources.
5. If your environment requires private API endpoints, custom account roles, private link, or tighter load balancer security groups, pass additional flags through `primary_additional_rosa_args` and `secondary_additional_rosa_args`.

## Suggested Overrides

Example with explicit extra ROSA flags:

```bash
terraform apply \
  -var primary_cluster_name=consul-rosa-east \
  -var secondary_cluster_name=consul-rosa-west \
  -var 'primary_additional_rosa_args=["--channel-group","stable"]' \
  -var 'secondary_additional_rosa_args=["--channel-group","stable"]'
```