{{- if and .Values.ai .Values.ai.enabled .Values.connectInject.enabled }}
# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0
# Generated by controller-gen. Manually maintained until code-gen pipeline is wired.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  labels:
    app: {{ template "consul.name" . }}
    chart: {{ template "consul.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
    component: crd
  name: agentconfigs.consul.hashicorp.com
spec:
  group: consul.hashicorp.com
  names:
    kind: AgentConfig
    listKind: AgentConfigList
    plural: agentconfigs
    singular: agentconfig
    shortNames:
      - agc
    categories:
      - all
  scope: Cluster
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Synced
          type: string
          description: Whether the controller has successfully reconciled this resource.
          jsonPath: .status.conditions[?(@.type=="Ready")].status
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          description: >-
            AgentConfig defines the cluster-wide defaults for the AI agent
            sidecar injected by the Consul connect-inject controller. When
            enabled, every annotated pod receives an agent init-container that
            handles interceptor proxying, MCP connectivity, and optional
            human-in-the-loop (HITL) approval.
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              description: Spec defines the desired state of AgentConfig.
              type: object
              required:
                - enabled
              properties:
                enabled:
                  description: >-
                    Enabled controls whether the AI agent sidecar feature is
                    active. When false the controller takes no action on
                    annotated pods.
                  type: boolean
                  default: false
                defaults:
                  description: >-
                    Defaults contains the cluster-wide default settings applied
                    to every agent-annotated pod unless overridden at the pod
                    annotation level.
                  type: object
                  properties:
                    interceptorPort:
                      description: >-
                        TCP port the agent interceptor listens on inside the
                        pod. Must not conflict with the application or Envoy
                        proxy port (20000).
                      type: integer
                      format: int32
                      default: 21101
                      minimum: 1024
                      maximum: 65535
                    mcpPort:
                      description: >-
                        TCP port used for MCP connectivity within the pod.
                      type: integer
                      format: int32
                      default: 15101
                      minimum: 1024
                      maximum: 65535
                    hitl:
                      description: >-
                        HITL contains human-in-the-loop approval settings for
                        the agent.
                      type: object
                      properties:
                        port:
                          description: >-
                            TCP port the HITL approval server listens on inside
                            the pod.
                          type: integer
                          format: int32
                          default: 16101
                          minimum: 1024
                          maximum: 65535
                        approvalTimeout:
                          description: >-
                            Duration the agent waits for a human approval
                            before timing out.
                          type: string
                          default: "60s"
                    resources:
                      description: >-
                        Resources defines the CPU/memory requests and limits for
                        the agent init-container injected into each annotated
                        pod.
                      type: object
                      properties:
                        requests:
                          description: Requests describes the minimum amount of compute resources required.
                          type: object
                          additionalProperties:
                            anyOf:
                              - type: integer
                              - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        limits:
                          description: Limits describes the maximum amount of compute resources allowed.
                          type: object
                          additionalProperties:
                            anyOf:
                              - type: integer
                              - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
            status:
              description: Status defines the observed state of AgentConfig.
              type: object
              properties:
                conditions:
                  description: Conditions describe the current conditions of the AgentConfig.
                  type: array
                  items:
                    type: object
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    properties:
                      lastTransitionTime:
                        type: string
                        format: date-time
                      message:
                        type: string
                        maxLength: 32768
                      observedGeneration:
                        type: integer
                        format: int64
                        minimum: 0
                      reason:
                        type: string
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      status:
                        type: string
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                      type:
                        type: string
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                lastSyncedTime:
                  description: LastSyncedTime is the last time the controller successfully reconciled this resource.
                  type: string
                  format: date-time
{{- end }}
