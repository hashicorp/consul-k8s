{{- if and .Values.ai .Values.ai.enabled .Values.connectInject.enabled }}
# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0
#
# This post-install/post-upgrade Job runs the "mcp-server-resources" subcommand.
# It reads ai.mcpServer defaults from values.yaml (passed as CLI flags below)
# and upserts the McpServerConfig CR in the cluster so that
# McpServerConfigController always has an up-to-date CR to reconcile.
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ template "consul.fullname" . }}-ai-mcp-server-resources
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "consul.name" . }}
    chart: {{ template "consul.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
    component: ai-mcp-server
    {{- if .Values.global.extraLabels }}
      {{- toYaml .Values.global.extraLabels | nindent 4 }}
    {{- end }}
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": hook-succeeded,before-hook-creation
spec:
  template:
    metadata:
      name: {{ template "consul.fullname" . }}-ai-mcp-server-resources
      labels:
        app: {{ template "consul.name" . }}
        chart: {{ template "consul.chart" . }}
        release: {{ .Release.Name }}
        component: ai-mcp-server
        {{- if .Values.global.extraLabels }}
          {{- toYaml .Values.global.extraLabels | nindent 8 }}
        {{- end }}
      annotations:
        "consul.hashicorp.com/connect-inject": "false"
        "consul.hashicorp.com/mesh-inject": "false"
    spec:
      restartPolicy: Never
      serviceAccountName: {{ template "consul.fullname" . }}-ai-mcp-server-resources
      containers:
        - name: ai-mcp-server-resources
          image: {{ .Values.global.imageK8S }}
          {{ template "consul.imagePullPolicy" . }}
          {{- include "consul.restrictedSecurityContext" . | nindent 10 }}
          command:
            - consul-k8s-control-plane
          args:
            - mcp-server-resources
            - -config-name=consul-mcp-server
            - -app={{ template "consul.name" . }}
            - -chart={{ template "consul.chart" . }}
            - -heritage={{ .Release.Service }}
            - -release-name={{ .Release.Name }}
            - -component=ai-mcp-server
            - -enabled={{ .Values.ai.enabled }}
            - -interceptor-port={{ .Values.ai.mcpServer.defaults.interceptorPort }}
            - -transport={{ .Values.ai.mcpServer.defaults.transport }}
            - -path={{ .Values.ai.mcpServer.defaults.path }}
            - -protocol-version={{ .Values.ai.mcpServer.defaults.protocolVersion }}
            - -requests-memory={{ .Values.ai.mcpServer.defaults.resources.requests.memory }}
            - -requests-cpu={{ .Values.ai.mcpServer.defaults.resources.requests.cpu }}
            - -limits-memory={{ .Values.ai.mcpServer.defaults.resources.limits.memory }}
            - -limits-cpu={{ .Values.ai.mcpServer.defaults.resources.limits.cpu }}
          resources:
            requests:
              memory: "50Mi"
              cpu: "50m"
            limits:
              memory: "50Mi"
              cpu: "50m"
      {{- if .Values.global.acls.tolerations }}
      tolerations:
        {{ tpl .Values.global.acls.tolerations . | indent 8 | trim }}
      {{- end }}
{{- end }}
