{{- if and .Values.ai .Values.ai.enabled .Values.connectInject.enabled }}
# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0
# Generated by controller-gen. Manually maintained until code-gen pipeline is wired.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  labels:
    app: {{ template "consul.name" . }}
    chart: {{ template "consul.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
    component: crd
  name: mcpserverconfigs.consul.hashicorp.com
spec:
  group: consul.hashicorp.com
  names:
    kind: McpServerConfig
    listKind: McpServerConfigList
    plural: mcpserverconfigs
    singular: mcpserverconfig
    shortNames:
      - msc
    categories:
      - all
  scope: Cluster
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Synced
          type: string
          description: Whether the controller has successfully reconciled this resource.
          jsonPath: .status.conditions[?(@.type=="Ready")].status
        - name: Transport
          type: string
          jsonPath: .spec.defaults.transport
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          description: >-
            McpServerConfig defines the cluster-wide defaults for the MCP
            (Model Context Protocol) server sidecar injected by the Consul
            connect-inject controller. When enabled, every annotated pod
            receives an interceptor init-container that exposes an
            MCP-compatible endpoint for AI tool/context routing.
          type: object
          properties:
            apiVersion:
              description: >-
                APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the
                latest internal value, and may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: >-
                Kind is a string value representing the REST resource this
                object represents. Servers may infer this from the endpoint
                the client submits requests to. Cannot be updated. In
                CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: Spec defines the desired state of McpServerConfig.
              type: object
              required:
                - enabled
              properties:
                enabled:
                  description: >-
                    Enabled controls whether the MCP server interceptor
                    feature is active. When false the controller takes no
                    action on annotated pods.
                  type: boolean
                  default: false
                defaults:
                  description: >-
                    Defaults contains the cluster-wide default settings
                    applied to every mcp-annotated pod unless overridden at
                    the pod annotation level.
                  type: object
                  properties:
                    interceptorPort:
                      description: >-
                        InterceptorPort is the TCP port the MCP server
                        container listens on inside the pod. Must not
                        conflict with the application port or the Envoy
                        proxy port (20000).
                      type: integer
                      format: int32
                      default: 21102
                      minimum: 1024
                      maximum: 65535
                    transport:
                      description: >-
                        Transport is the MCP wire transport to use.
                      type: string
                      default: streamable-http
                      enum:
                        - streamable-http
                        - sse
                        - stdio
                    path:
                      description: >-
                        Path is the HTTP path the MCP server listens on.
                      type: string
                      default: "/mcp"
                    protocolVersion:
                      description: >-
                        ProtocolVersion is the MCP protocol version the
                        server advertises.
                      type: string
                      default: "2025-03-26"
                    resources:
                      description: >-
                        Resources defines the CPU/memory requests and limits
                        for the MCP server init-container injected into each
                        annotated pod.
                      type: object
                      properties:
                        requests:
                          description: Requests describes the minimum amount of compute resources required.
                          type: object
                          additionalProperties:
                            anyOf:
                              - type: integer
                              - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        limits:
                          description: Limits describes the maximum amount of compute resources allowed.
                          type: object
                          additionalProperties:
                            anyOf:
                              - type: integer
                              - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
            status:
              description: Status defines the observed state of McpServerConfig.
              type: object
              properties:
                conditions:
                  description: Conditions describe the current conditions of the McpServerConfig.
                  type: array
                  items:
                    description: Condition contains details for one aspect of the current state of this API Resource.
                    type: object
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    properties:
                      lastTransitionTime:
                        description: >-
                          LastTransitionTime is the last time the condition
                          transitioned from one status to another.
                        type: string
                        format: date-time
                      message:
                        description: Message is a human-readable message indicating details about the transition.
                        type: string
                        maxLength: 32768
                      observedGeneration:
                        description: ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        type: integer
                        format: int64
                        minimum: 0
                      reason:
                        description: Reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        type: string
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      status:
                        description: Status of the condition, one of True, False, Unknown.
                        type: string
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                      type:
                        description: Type of condition in CamelCase.
                        type: string
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                lastSyncedTime:
                  description: LastSyncedTime is the last time the controller successfully reconciled this resource.
                  type: string
                  format: date-time
{{- end }}
