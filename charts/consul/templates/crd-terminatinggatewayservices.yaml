{{- if and .Values.connectInject.enabled .Values.global.peering.enabled }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: terminatinggatewayservices.consul.hashicorp.com
  labels:
    app: {{ template "consul.name" . }}
    chart: {{ template "consul.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
    component: crd
spec:
  group: consul.hashicorp.com
  names:
    kind: TerminatingGatewayService
    listKind: TerminatingGatewayServiceList
    plural: terminatinggatewayservices
    singular: terminatinggatewayservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The sync status of the resource with Consul
      jsonPath: .status.conditions[?(@.type=="Synced")].status
      name: Synced
      type: string
    - description: The last successful synced time of the resource with Consul
      jsonPath: .status.lastSyncedTime
      name: Last Synced
      type: date
    - description: The age of the resource
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TerminatingGatewayService is the Schema for the terminatinggatewayservices
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TerminatingGatewayServiceSpec defines the desired state of
              TerminatingGatewayService.
            properties:
              service:
                description: Service contains information about a service, needed
                  for registration or de-registration.
                properties:
                  address:
                    description: Address specifies the address to register.
                    type: string
                  datacenter:
                    description: Datacenter specifies the datacenter, which defaults
                      to the agent's datacenter if not provided.
                    type: string
                  node:
                    description: Node specifies the node ID to register.
                    type: string
                  nodeMeta:
                    additionalProperties:
                      type: string
                    description: NodeMeta specifies arbitrary KV metadata pairs for
                      filtering purposes.
                    type: object
                  serviceAddress:
                    description: ServiceAddress specifies the address of the service.
                      If not provided, the agent's address is used as the address
                      for the service during DNS queries.
                    type: string
                  serviceEnableTagOverride:
                    description: ServiceEnableTagOverride specifies to disable the
                      anti-entropy feature for this service's tags. If set to true
                      then external agents can update this service in the catalog
                      and modify the tags.
                    type: boolean
                  serviceId:
                    description: ServiceID specifies to register a service. If ID
                      is not provided, it will be defaulted to the value of the ServiceName
                      property.
                    type: string
                  serviceMeta:
                    additionalProperties:
                      type: string
                    description: SericeMeta specifies arbitrary KV metadata linked
                      to the service instance.
                    type: object
                  serviceName:
                    description: ServiceName specifies the logical name of the service.
                    type: string
                  servicePort:
                    description: ServicePort specifies the port of the service.
                    type: integer
                  serviceTags:
                    description: ServiceTags specifies a list of tags to assign to
                      the service. These tags can be used for later filtering and
                      are exposed via the APIs.
                    items:
                      type: string
                    type: array
                  taggedAddresses:
                    additionalProperties:
                      type: string
                    description: TaggedAddresses specifies the tagged addresses.
                    type: object
                type: object
            type: object
          status:
            description: TerminatingGatewayServiceStatus defines the observed state
              of TerminatingGatewayService.
            properties:
              conditions:
                description: Conditions indicate the latest available observations
                  of a resource's current state.
                items:
                  description: 'Conditions define a readiness condition for a Consul
                    resource. See: https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#typical-status-properties'
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              lastSyncedTime:
                description: LastSyncedTime is the last time the resource successfully
                  synced with Consul.
                format: date-time
                type: string
              serviceInfoRef:
                description: 'Important: Run "make" to regenerate code after modifying
                  this file ServiceInfoRefStatus shows information about the service.'
                properties:
                  service:
                    type: string
                  serviceInfo:
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
{{- end }}
