# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0

apiVersion: v1
kind: Service
metadata:
  name: multiport
spec:
  selector:
    app: multiport
  ports:
    - name: web
      port: 8080
      targetPort: 8080
    - name: admin
      port: 9090
      targetPort: 9090
