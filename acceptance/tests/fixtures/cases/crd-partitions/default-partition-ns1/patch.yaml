apiVersion: consul.hashicorp.com/v1
kind: ExportedServices
metadata:
  name: default
spec:
  services:
  - name: mesh-gateway
    namespace: default
    consumers:
    - partition: secondary
  - name: static-server
    namespace: ns1
    consumers:
    - partition: secondary
