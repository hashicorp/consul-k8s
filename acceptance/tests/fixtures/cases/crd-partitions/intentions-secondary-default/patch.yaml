apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceIntentions
metadata:
  name: intentions
spec:
  destination:
    name: static-server
    namespace: default
  sources:
  - name: static-client
    namespace: default
    partition: default
    action: allow
