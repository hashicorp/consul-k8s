# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# SINGLE-ext-proc gateway scenario. Apply AFTER ../common (which provides the
# backends, processors, mesh services, protocols and intentions this references).
resources:
  - gateway.yaml
  - servicedefaults.yaml
  - routes.yaml
