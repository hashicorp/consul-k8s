# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0

# Calico installation for dc4 – IPAM pool 192.171.0.0/16
# Must match the podSubnet in kind-4.config.
apiVersion: operator.tigera.io/v1
kind: Installation
metadata:
  name: default
spec:
  calicoNetwork:
    ipPools:
    - blockSize: 26
      cidr: 192.171.0.0/16
      encapsulation: VXLANCrossSubnet
      natOutgoing: Enabled
      nodeSelector: all()

---

apiVersion: operator.tigera.io/v1
kind: APIServer
metadata:
  name: default
spec: {}
